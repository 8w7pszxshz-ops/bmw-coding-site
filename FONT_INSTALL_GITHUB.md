# 📝 Инструкция: Как добавить шрифт через GitHub

## Способ 1: Через веб-интерфейс GitHub (самый простой)

### Шаг 1: Скачай шрифт на компьютер
1. Открой: https://preview--bmw-coding-site.poehali.dev/download-font-tool.html
2. Нажми кнопку "Скачать шрифт"
3. Файл `Reborn-Technologies.ttf` сохранится на твой компьютер

### Шаг 2: Загрузи файл в GitHub
1. Зайди в свой GitHub репозиторий проекта
2. Открой папку `public/`
3. Создай папку `fonts` (если её нет):
   - Нажми **Add file** → **Create new file**
   - В поле имени файла напиши: `fonts/.gitkeep`
   - Нажми **Commit changes**
4. Зайди в папку `public/fonts/`
5. Нажми **Add file** → **Upload files**
6. Перетащи файл `Reborn-Technologies.ttf` или нажми "choose your files"
7. Напиши коммит: "Add Reborn Technologies font"
8. Нажми **Commit changes**

### Шаг 3: Синхронизируй с poehali.dev
1. В редакторе poehali.dev нажми **Скачать** → **Синхронизировать с GitHub**
2. Подожди несколько секунд
3. Готово! Шрифт появится в проекте и будет работать на сайте 🚀

---

## Способ 2: Через Git командную строку (для продвинутых)

```bash
# 1. Склонируй репозиторий (если ещё не сделал)
git clone https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
cd ВАШ_РЕПОЗИТОРИЙ

# 2. Создай папку для шрифтов
mkdir -p public/fonts

# 3. Скачай шрифт напрямую в проект
curl -o public/fonts/Reborn-Technologies.ttf "https://cdn.poehali.dev/projects/892585f1-24a2-432b-810c-dd69d2686659/bucket/Reborn%20Technologies.ttf"

# 4. Добавь файл в Git
git add public/fonts/Reborn-Technologies.ttf

# 5. Сделай коммит
git commit -m "Add Reborn Technologies font"

# 6. Отправь на GitHub
git push origin main
```

После этого синхронизируй проект в poehali.dev через **Скачать** → **Синхронизировать с GitHub**.

---

## Способ 3: GitHub Desktop (для пользователей Windows/Mac)

1. Открой GitHub Desktop
2. Выбери свой репозиторий
3. Создай папку `public/fonts/` в файловом менеджере
4. Скопируй файл `Reborn-Technologies.ttf` в папку `public/fonts/`
5. GitHub Desktop автоматически увидит изменения
6. Напиши коммит: "Add Reborn Technologies font"
7. Нажми **Push origin**
8. Синхронизируй в poehali.dev через **Скачать** → **Синхронизировать с GitHub**

---

## Проверка установки

После синхронизации проверь:
1. В редакторе poehali.dev должен появиться файл `public/fonts/Reborn-Technologies.ttf`
2. Открой сайт и зайди в раздел **ChipTuning** — шрифт должен отображаться
3. Если шрифт не работает, открой F12 → Console — ошибок быть не должно

---

## Что уже настроено в проекте?

✅ В файле `src/index.css` уже прописан путь к шрифту:
```css
@font-face {
  font-family: "Reborn Technologies";
  src: url("/fonts/Reborn-Technologies.ttf") format("truetype");
}
```

✅ В компоненте ChipTuning шрифт уже применён:
```css
font-family: 'Reborn Technologies', sans-serif;
```

Остаётся только **добавить файл шрифта** — и всё заработает! 🎯
