# 🔧 Гайд по тестированию мигалок чип-тюнинга

## Что было исправлено:
1. ✅ Мигалки работают 6.5 секунд (не 15)
2. ✅ Показываются только при первом открытии диалога в сессии
3. ✅ Используется sessionStorage вместо useRef (работает через unmount)
4. ✅ CSS правильный - диалог не замирает после анимации
5. ✅ Всё кликабельно и работает в любой момент

---

## 🎯 Как проверить (пошагово):

### Вариант 1: Автоматический тест
1. Открой в браузере: `https://preview--bmw-coding-site.poehali.dev/test-chiptuning.html`
2. Смотри результаты тестов
3. Если все ✅ — технически всё работает

### Вариант 2: Ручная проверка
1. **Очисти кэш браузера**: Ctrl+Shift+R (или Cmd+Shift+R на Mac)
2. Открой консоль браузера (F12)
3. Перейди в калькулятор Чип-тюнинга
4. Жди диалог выбора серии

**Что должно произойти:**
- В консоли увидишь: `[CHIPTUNING DEBUG] ✅ Запускаю мигалки на 6.5 сек`
- Диалог будет мигать синими и красными огнями
- Через 6.5 секунд в консоли: `[CHIPTUNING DEBUG] ⏱️ 6.5 сек прошло - выключаю мигалки`
- Мигалки исчезнут, диалог станет нормальным
- Всё работает: скролл, кнопки, крестик

5. **Выбери серию** (например, 3 Series)
6. Диалог закроется → откроется выбор кузова
7. **Нажми кнопку "Назад"**
8. Диалог серий откроется снова

**Что должно произойти:**
- В консоли: `[CHIPTUNING DEBUG] ⏩ Мигалки уже были показаны, пропускаю`
- Диалог БЕЗ мигалок, сразу рабочий
- Всё кликабельно

---

## 🧪 Проверка sessionStorage

Открой консоль браузера (F12) и выполни:

```javascript
// Проверить значение
sessionStorage.getItem('chiptuning-lights-shown')
// Должно быть: "true" (если мигалки уже показывались)
// или: null (если ещё не показывались)

// Очистить (чтобы мигалки показались снова)
sessionStorage.removeItem('chiptuning-lights-shown')

// Перезагрузить калькулятор
location.reload()
```

---

## 📊 Debug логи в консоли

При открытии диалога увидишь:
```
[CHIPTUNING DEBUG] step: series | hasShown: null | showPoliceLights: false
[CHIPTUNING DEBUG] ✅ Запускаю мигалки на 6.5 сек
[CHIPTUNING DEBUG] ⏱️ 6.5 сек прошло - выключаю мигалки
```

При повторном открытии:
```
[CHIPTUNING DEBUG] step: series | hasShown: true | showPoliceLights: false
[CHIPTUNING DEBUG] ⏩ Мигалки уже были показаны, пропускаю
```

---

## 🐛 Если что-то не работает:

### Проблема: Мигалки не появляются вообще
**Решение:**
```javascript
// В консоли браузера:
sessionStorage.removeItem('chiptuning-lights-shown')
location.reload()
```

### Проблема: Мигалки появляются каждый раз
**Проверь:**
```javascript
sessionStorage.getItem('chiptuning-lights-shown')
// Если null каждый раз — значит браузер блокирует sessionStorage
```

### Проблема: Диалог не закрывается / не работают кнопки
**Проверь в консоли:**
- Должно быть: `⏱️ 6.5 сек прошло - выключаю мигалки`
- НЕ должно быть ошибок CSS или JavaScript

### Проблема: Старый код
**Решение:**
1. Ctrl+Shift+R (полная перезагрузка)
2. Очисти кэш сайта в настройках браузера
3. Открой в режиме инкогнито

---

## ✅ Критерии успеха:

- [x] Мигалки работают ровно 6.5 секунд
- [x] Показываются только при первом открытии в сессии
- [x] После мигалок всё работает (скролл, кнопки, крестик)
- [x] При возврате "Назад" → диалог без мигалок
- [x] Диалог закрывается при выборе серии
- [x] В консоли правильные debug логи

---

## 🚀 Финальная проверка:

1. Открой сайт в **режиме инкогнито** (Ctrl+Shift+N)
2. Открой консоль (F12)
3. Зайди в Чип-тюнинг
4. Смотри логи и поведение
5. Всё должно совпадать с описанием выше

---

**Commit:** e303962  
**Файлы:** ChipTuningMobileView.tsx, index.css  
**Метод хранения:** sessionStorage (ключ: `chiptuning-lights-shown`)
